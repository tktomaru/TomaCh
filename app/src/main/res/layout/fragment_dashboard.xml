<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.dashboard.DashboardFragment">

    <!-- ラベル -->
    <TextView
        android:id="@+id/tvApiLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="OpenAI API Key"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="24dp"/>

    <!-- API キー入力欄 -->
    <EditText
        android:id="@+id/etApiKey"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="sk-*********************"
        android:inputType="textVisiblePassword"
        app:layout_constraintTop_toBottomOf="@id/tvApiLabel"
        app:layout_constraintStart_toStartOf="@id/tvApiLabel"
        app:layout_constraintEnd_toEndOf="@id/tvApiLabel"
        android:layout_marginTop="8dp"/>

    <!-- 保存ボタン -->
    <Button
        android:id="@+id/btnSaveApiKey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="保存"
        app:layout_constraintTop_toBottomOf="@id/etApiKey"
        app:layout_constraintEnd_toEndOf="@id/etApiKey"
        android:layout_marginTop="16dp"/>

    <!-- 英訳表示オン/オフ用スイッチ -->
    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/switchEnglish"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="英訳表示"
        android:checked="true"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnSaveApiKey"
        app:layout_constraintStart_toStartOf="@id/etApiKey"/>

</androidx.constraintlayout.widget.ConstraintLayout>
